# TRAPS.md — tamkorea.com 웹에서 실제로 겪은 함정

> 규약: 실제로 겪은 것만 적는다. 추측은 "미검증" 표기. (루트 CLAUDE.md §7)

## 팀명생성기 · 예약테이블 의존성 감사 (2026-08-13 실측)

Owner 지시로 예약테이블(Shadow Group) 폐기 전 전수 감사를 실행한 결과.

### 확정 사실

- **팀명생성기 수식은 예약테이블과 무관하다.** Meta API `referencedFieldIds` 실측:
  예약입력_DB·진행_DB_OLD 양쪽 모두 `고객명(lookup)·지점명(lookup)·예약일시·대표인플`만 참조.
  lookup 경유는 매장코드→CS_DB 다. → **예약테이블을 삭제해도 팀명생성기는 계속 생성된다.**
- **단, AND 조건이라 대표인플이 비면 키 자체가 안 생긴다.** 몽그레 기자 20건(수동입력)이
  그 사례 — 팀명생성기 공백이라 웹의 예약입력_DB 매칭이 영원히 실패한다.
  **수동으로 진행_DB에 입력할 때는 대표인플을 채워야 한다.**
- **팀 묶음 키(teamId)가 예약테이블 레코드 ID였다.** try/catch 로 500은 막아도,
  테이블이 삭제되면 링크 필드가 통째로 사라져 2인 팀이 달력에 낱개로 흩어진다.
  → `d5314de` 에서 링크→팀명생성기→자기ID 3단 폴백으로 교체 (오늘 동작 불변).
- **recruiter-schedule 은 예약테이블이 본문의 주 소스였다** (client-schedule 은 c865d2c
  에서 이미 예약입력_DB로 이관). → `d5314de` 에서 같은 이관 적용.
- 대표인플 복합키(매장+날짜+대표인플)로 문자열 키를 대체하는 안은 **기각** —
  2026.8월 실측 매칭 88% (팀명생성기 98%), 두 테이블의 대표인플 값 불일치 19건.

### 예약테이블 삭제 전 남은 조건 (Owner 작업)

1. **오토메이션 확인** — 진행_DB→예약테이블 동기화 오토메이션이 남아 있으면 삭제 후
   매 실행마다 에러가 쌓인다. API로는 오토메이션을 못 읽으므로 UI에서 직접 꺼야 한다.
2. **(미사용) 표기 lookup 4개 삭제** — 진행_DB_OLD·예약테이블의 XHS/DP_플랫폼(미사용).
   필드 삭제는 API 불가, UI에서만 가능.
3. 삭제하면 옛 달 일부 건의 인원·메시지 폴백이 사라진다
   (예약입력_DB 매칭률 실측: 8월 98% · 7월 87% · 6월 83% — 나머지가 폴백 의존분).

### 링크 필드 지형 (Meta 실측)

예약테이블을 가리키는 링크: Campaign_DB·INFL_DB·진행_DB_OLD×2·예약입력_DB×2·진행_DB — 7개.
전부 테이블 삭제와 함께 자동 소멸하고, 이를 경유하는 lookup/rollup 은 (미사용) 2개뿐이라
다른 테이블의 필드 손상은 없다. **예약입력_DB↔예약테이블 링크는 2,782건 전부 공백**이라
lookup 체인으로 값을 흘리려는 시도는 반드시 실패한다 (2026-08-13 실측 — 실제로 한 번 실패했다).

## Airtable 조회 함정 (이 저장소에서 실측)

- 진행_DB_OLD 대량 필터는 `is none of` 제외 방식으로 — 빈값 레코드가 173건이라
  포함 방식은 조용히 누락된다 (루트 CLAUDE.md §4와 동일, 웹 API에서도 재확인).
- `String(v).replace(/'/g, "\'")` 는 no-op — escFormula 는 백슬래시부터 늘린다.
  client-partner.js 에 실존했던 버그.
