# STAFF_WEB_HANDOVER — 담당자 웹 (Softr 대체) 전체 구조·설계·결정 기록

*작성 2026-08-05 · Claude (Fable 5) · AG(Antigravity) 공동 검토용*
*실화면 캡처: [docs/staff_screens/](docs/staff_screens/) (로컬 dev, 실데이터 렌더)*

---

## 0. 이 문서를 읽는 법

- **무엇을 만들었나**만 보려면 → §5 (화면별 설계)
- **왜 그렇게 만들었나**를 검증하려면 → §6 (검토·결정 로그) — 쟁점별 대안·결론·근거
- **깨뜨리면 안 되는 것**을 확인하려면 → §4 (예약봇 계약) + §8 (함정)
- 코드 리뷰 진입점 → §7 (API 레퍼런스) + §5.5 파일 구성

전제 지식 문서 (이 문서가 요약·참조하는 원본):
- `계약관리/SYSTEM_DOSSIER_2026-08-04.md` — 시스템 전경 지도 (입력→원장→집계→표출)
- `03_Epic_Automation_RPA/TK_Resv_V7_App/README.md` — 예약봇 상태 라이프사이클·Softr 버튼 명세 원본
- `02_Epic_NoCode_DB/softr_input_system_final/Softr_Input_System_Master_Guide.md` — 4월 Softr 통합 설계
- `02_Epic_NoCode_DB/softr_ui_improvement_action_plan.md` — 5월 3트랙(Softr 보강/대안 도구/자체웹) 비교 검토
- `02_Epic_NoCode_DB/TK_DB_V3/_raw_schema.json` — Airtable 실측 스키마 덤프 (2026-08-03)

---

## 1. 경위 — 왜 Softr를 대체하는가

### 타임라인

| 시기 | 사건 |
|---|---|
| 2026-04 | TAM-LINK 구축 (Airtable + Softr). 하향식 입력·팀 쪼개기 자동화·매직링크 설계 (`System_Build_Guide.md`) |
| 2026-05-06 | Softr 한계 봉착 → 3트랙 병렬 검토: A) Softr v1 완성 B) 대안 노코드(Noloco/JetAdmin/Retool) C) 자체 서브페이지 PoC (`softr_ui_improvement_action_plan.md`) |
| 2026-05-18 | **결정: Softr 유지.** 자체 웹은 "일괄 이전 시점"으로 연기 |
| 2026-05~07 | 고객·협력사 표출은 자체 웹으로 선행 이전 완료 — /schedule /partner /report /manager + 관리자 4화면(/admin~) + API 15개 |
| 2026-08-04 | **결정 번복 (Owner): 담당자 화면도 자체 웹으로.** 이유: ① 현황 가시성 부족 ② 정산월 오입력 방지 불가 ③ Softr 기능 제약으로 개선 시도가 계속 막힘. 5/18과 달리 지금은 자체 웹 인프라가 이미 커져 있어 "일괄 이전 시점"이 사실상 도래했다고 판단 |
| 2026-08-04~05 | Phase 1(보드) → Phase 2(예약폼) → Phase 3~4(발송큐·인플등록) 구현·배포 (이 문서 시점) |

### Owner가 지목한 페인포인트 (원문 취지)

1. 담당자 업무조회 화면이 가로 스크롤 지옥이라 **한눈에 현황 파악이 안 된다**
2. 예약입력 때 해당 고객사의 **월 목표 대비 진행이 안 보여서**, 7월 목표가 안 찼는데 8월 정산월로 입력하는 오류가 잦다 (정산월 오류 = 고객사 보고서 오류로 직결)
3. 목표대비 섭외·업로드 현황 화면이 파악이 어렵고 **업로드 지연 관리가 안 된다**
4. Softr 안에서 이것저것 시도했지만 제약이 많아 거의 해결 불가
5. Softr의 장점(블록 조립식 유지보수)은 인정 — 자체 웹 유지보수 부담은 알고 감수 (완화책: 기존 패턴 복제·필드명 상수화·이 문서)

---

## 2. 시스템 전경 — 데이터 흐름과 테이블

### 2.1 흐름 전경 (도시어 요약)

```
[입력]                          [원장]                     [집계]               [표출]
Softr 폼 ─┐
웹 /staff/new ├→ 예약입력_DB ──(팀명생성기)──→ 진행_DB_OLD ─┐             ┌→ /schedule  고객 달력·실적
카톡 수동 ─┘   (팀 단위 원본)    [Airtable 자동화 분할]      │             ├→ /partner   협력사 묶음
                    ↑                              정산월(수동)│             ├→ /report    고객 리포트
              예약봇 TK_Resv_V7                        ↓      │             ├→ /manager   (구) 섭외담당
              (카톡 발송+상태 캐스케이드)      입력 정산월(수식)             ├→ /staff     ★ 신규 보드
                    ↑                                  ↓      │             ├→ /staff/queue ★ 발송큐
              웹 /staff/queue ──(자동발송체크)──  [Airtable 자동화]         └→ /admin     목표·실적 관리
                                                       ↓
                                              귀속 정산월(링크) → Campaign_DB (rollup 방문·업완·취소)
```

**핵심 명제 (도시어 원문): "정산월 하나가 틀리면 고객사 보고서가 틀린다."**
이 시스템의 모든 함정은 이 한 줄로 수렴하며, 웹 설계의 최우선 목표도 이것이었다.

### 2.2 핵심 조인 키 2개

| 키 | 형식 | 잇는 것 |
|---|---|---|
| `팀명생성기` | `{매장명}_MMDD_{XHS_ID}` (formula) | 예약입력_DB(팀 1건) ↔ 진행_DB_OLD(건 N개). 예약봇 캐스케이드·웹 삭제 동반정리가 이 키로 exact 매칭 |
| `귀속 정산월` | Link (진행_DB_OLD → Campaign_DB) | 개별 예약 ↔ 월별 계약. **자동화가 채운다** — 사람이 정산월을 입력하면 `입력 정산월` 수식(`{고객명}&{지점명}&" ["&{정산월}&"]"`)이 완성되고, 자동화가 계약명 문자열 매칭으로 링크를 건다 |

### 2.3 테이블별 요점 (Base `appdsAV2ewZWCkyIa`, 테이블 10개 중 웹이 쓰는 5개)

**Campaign_DB — 고객사×계약월 (약 500건, 96필드)**
- `계약명(수식)` = `{고객사명}{지점명} [{계약월}]` (공백 없이 붙음 — 자동화 매칭 키)
- rollup 6종: `인플_방문/업완/취소`, `체험_방문/업완/취소` + `기자_실적`. **rollup 조건은 "is none of" 제외 방식** (진행상태 빈값 173건 때문에 포함 방식이 아니라 제외 방식으로 설계됨)
- `합산_목표(수식)` = 인플목표×330,000 + 체험목표×50,000 + 기자목표×50,000 (돈 단위 — 관리자용. 담당자 화면에는 안 내림)
- `업체명(링크)` → CS_DB (고객사명·지점명 룩업의 원천이자 웹의 업체정보 조인 키)
- `추가체험단(checkbox)` = 목표 초과 추가 섭외 가능 / `확인요망(checkbox)`·`전달사항(formula)` = 관리자↔섭외자 소통
- ⚠️ 실측 (2026-08-04): **8월 계약 95건 중 75건이 계약유형 공란.** 그중 5건은 체험 실적 보유 → §6-D7

**진행_DB_OLD — 개별 예약 원장 (3,216건, 62필드)**
- `정산월(단일선택)` ← 사람이 입력. 모든 것의 출발점 / `(원)정산월` = 원본 보존용
- ⚠️ 필드명에 공백: `입력 정산월` `귀속 정산월` — 과거 941건 오집계 사고의 원인
- 웹이 읽는 것: 상태 2종(`진행상태`, `제출상태(formula — 미제출 시 "🔴")`), 결과링크 3종(`XHS_Result` `DP_Result` `DY_Result`), 인플(`XHS_ID` 룩업, `대표인플_ID` 룩업(2026-08-04 신설 — 스키마 덤프에는 아직 없음), `XHS_link1 (from WC_ID_)` = 샤오홍슈 홈), 건수(`총인원` `변경인원` `XHS_건수` `DP_건수`), `인플전달링크(rollup)` = /submit 제출 URL, `拍摄剧本(룩업)` = 촬영대본
- 웹이 쓰는 것: **`비고`(richText) 하나** (메모 편집). 삭제는 부모 삭제 시 동반 정리만

**예약입력_DB — 팀 단위 입력 원본 (61필드)**
- 선택지 실측: `예약_ID` HH|LH|AN|FB · `유형` 체험|인플|기자|체험→인플|기자→인플|기자→체험 · `진행상태` 13종 · `정산월` **2026. 3월~8월만 존재** (9월 옵션은 사람이 추가해야 함 — API로 불가)
- `예약메시지(formula)` `변경메시지(formula)` — 발송문. **formula라 쓰기 불가** (봇도 못 씀)
- `자동발송체크(checkbox)` — 봇 트리거 (§4)
- 웹 create가 쓰는 필드: 매장코드(링크)·예약_ID·유형·진행상태·정산월·예약일시·총인원·XHS_건수·DP_건수·대표인플(링크)·XHS_ID_(링크, 참여자 배열)·인원메모·고객전달메모·영문이름·비고

**CS_DB — 고객사 마스터 (72필드)**
- 웹이 읽는 것: `고객사명(필수)` `지점명(필수)` `중문명` `영업시간(필수)` `브레이크타임(필수)` `피크타임` `정기휴무(multiSelect)` `방문가능시간` `제공내역` `섭외주의사항` `비고` `拍摄剧本`
- `Campaign_DB(링크)` + 오타 쌍둥이 `Campain_DB(링크)` — 매장↔계약 조인은 이 링크로 (§6-D9)
- `사용여부(checkbox)` — 웹 매장 목록은 **미사용도 내려보내되 하단 배치·흐림 표시** ("숨기면 또 안 보인다 소리 나온다" — D7 학습)

**INFL_DB — 인플루언서 마스터 (966건 실측, 29필드)**
- 필수 5: `XHS_ID(필수)` `섭외_ID(필수)` `유형(필수)` `XHS_link1(필수)` `PAL(필수, 팔로워수)`
- `인플전용링크(formula)` = 개인 매직링크. 중복 등록 방지가 마스터 설계 원칙 (4월 설계부터)

---

## 3. Softr 앱 전수 분석 (2026-08-05 캡처 8장 기준)

| # | 화면 | 테이블 | 필드/버튼 상세 (캡처 확인) |
|---|---|---|---|
| ① | 체험단 진행현황 | Campaign_DB | 컬럼: 区域·중문명·업체명·목표·예약·업로드·진도·확인요망(체크)·비고·계약월·예약달력/제출링크·전달사항. 필터: 계약월·업체명·지역구분·区域 |
| ② | 담당자 업무조회 | 진행_DB_OLD | 컬럼 19개(경과일·预约状态·대표인플·XHS_ID·인플전달링크·小红书/大众/抖音·探店时间·변경일시·인수·小红/大众 건수·小红书主页·정산월·메모). Item 버튼 6개: 🚨취소/노쇼 요청·월/ID 수정(Edit)·🔗링크 제출(Edit)·➡인플전달링크복사(Open URL)·📝메모(Edit)·상태변경(Edit) — **전부 진행_DB_OLD 직접 수정, 발송 없음** |
| ③ | 신규예약 폼 (팝업) | 예약입력_DB | 필수: 매장코드·예약_ID·유형·진행상태·정산월·探店日期时间·人数·发布小红书(XHS_건수)·发布大众点评(DP_건수)·小红书账号(XHS_ID_, 여러 명)·预约 小红书账号(대표인플). 선택: 예약메모·영문이름(잠수함 동행 필수 안내문 포함). 상단에 커스텀 코드로 고객정보 블록 강제 삽입 (M1971 캡처) |
| ④ | 신규인플 폼 (팝업) | INFL_DB | 필수: 섭외_ID·유형·小红书账号·小红书链接·小红书粉丝. 선택: 연락처·영문이름·预约微信·微信群名称·닉네임·지역·大众点评账号 |
| ⑤ | 인플 조회 | INFL_DB | 컬럼: 섭외_ID·유형·XHS_ID·XHS_link1·PAL·인플전용링크·지역 |
| ⑥ | 예약 메시지 전송 | 예약입력_DB | 카드형. 필터 = **진행상태 예약요청 & 미발송만** 표시. 정렬 Shoot_ID Z→A. 버튼: 전송(Edit=자동발송체크 ON)·수정(Edit)·삭제(**Delete record**) |
| ⑦ | 변경 메시지 전송 | 예약입력_DB | 테이블형. 예약메시지·변경메시지 표시. 버튼: 변경/취소/노쇼/확정(Edit = 진행상태+자동발송체크+α) |
| ⑧ | 고객등록 (어드민) | CS_DB | 캡처 없음 (Owner 언급만) |

Softr 블록 설정 원본은 Softr 안에만 있음 — 위 캡처 8장이 사실상의 백업. (원본 캡처는 채팅 기록,
요약은 이 표.)

---

## 4. 예약봇 TK_Resv_V7 계약 — 절대 침범 금지선

봇은 `예약입력_DB.자동발송체크`(checkbox)를 **폴링**한다 (`remote_trigger_field`). 체크된 건을
상태별로 처리한 후 진행_DB_OLD를 `팀명생성기` exact 매칭으로 캐스케이드한다.

### 상태 라이프사이클 (V7 README 원문 표)

| 진행상태 | 발송 필드 | 메인 DB 처리 | 진행_DB_OLD 캐스케이드 |
|---|---|---|---|
| 예약요청 | `예약메시지` | → 예약확정 | → 예약확정 |
| 긴급예약 | `예약메시지` + 긴급 태그 + 추가 메시지 | → 예약확정 | → 예약확정 |
| 변경요청 | `변경메시지` (⚠차단 로직) | 발송 후 **자동으로 변경확정 전환** (V6.3) | → 변경확정 |
| 변경확정 | ❌ 발송 없음 | 상태만 변경확정. **예약일시 원본 유지·변경필드 유지** (V6.3 방안 A — README의 "예약일시←변경일시 이관"은 낡은 초기 설계) | → 변경확정 + 변경필드 복사 |
| 취소_방문자 / 취소_고객사 / 노쇼 | `변경메시지` 우선, 없으면 `예약메시지` 폴백 + **상태별 전용 템플릿** | 상태 재확인 PATCH | → 해당 상태 |

코드 실측 (TK_Resv_V7.py 2,041줄, 2026-08-05 검사):
- 폴링 61초 간격. **수집 즉시 자동발송체크 해제**(L1975, 중복 방지) → 발송 → 상태 PATCH → 캐스케이드
- 실패 시 재시도 3회 + 관리자 카톡 통보 (V7.2 — 2026-08-03 제주육림 무통보 소실 사고 후 도입)
- 변경요청은 변경메시지 Formula 재계산 지연 시 3초 대기 후 1회 재조회
- 취소_고객사는 봇 템플릿·예약입력_DB·진행_DB_OLD 옵션 모두 존재 — 완전 지원 확인

- **⚠차단 로직**: 변경일시 없이 변경요청하면 `변경메시지` formula가 경고문을 반환하고 봇이 발송을 차단
- **취소 안내문은 DB에 남지 않는다** — 봇이 발송 직전 조립 후 버림. 그래서 고객 달력엔 `api/_resv-message.js`가 봇과 같은 규칙으로 복원 표시 (2026-08-05 커밋 `2d4e4c9`)
- Softr 모달 버튼 명세 (V7 README "Softr 모달" 절 — 웹 큐가 그대로 계승): 수정=변경일시+변경인원+상태 변경요청+체크 / 변경확정=상태+체크 / 노쇼·취소=상태+고객전달메모+체크

### 웹이 지켜야 하는 4개 금지선

1. 신규 예약은 **예약입력_DB에만** 생성 (진행_DB_OLD 직접 생성 금지 — 분할 자동화·팀명생성기·봇이 전부 그 경로)
2. 발송 트리거는 **자동발송체크 하나** (다른 방법으로 발송 유발 금지)
3. 변경요청은 **변경일시 필수** (봇 차단과 서버 400 이중 방어)
4. 상태 문자열·필드명은 봇 config(`airtable_config_v7.json`)와 동일하게

---

## 5. 자체 웹 설계

### 5.1 설계 원칙 (2026-08-04 설계검토에서 확정)

1. **쓰기 경로가 설계의 심장** — UI보다 먼저 "어느 테이블에 누가 쓰는가"를 고정. Softr가 쓰던 경로를 1:1 계승
2. **재사용 우선** — admin-targets(월 계산·elapsed·유령 필터·rate limit 청크), recruiter-schedule(조회 골격), _admin-auth(게이트 패턴), 5/6 Track C 설계(슬라이드오버·컬러칩) 재활용. 실제로 부품의 60~70%가 기존 자산
3. **Softr 8화면 → 웹 3화면 압축** — ②⑥⑦은 같은 필드(진행상태·자동발송체크·메시지)를 만지므로 큐 하나로 통합. ④⑤는 별도 페이지가 아니라 폼 안 인라인 모달 (Softr에선 페이지를 갔다 와야 했음)
4. **단계 도입** — 읽기 전용(보드) → 쓰기 1개(폼) → 쓰기 전체(큐). 각 단계 배포 후 실측 피드백으로 다음 단계 수정 (실제로 매 단계 Owner 피드백로 재설계됨 — §6)

### 5.2 `/staff` 진도 보드 (Phase 1)

**목적**: 페인포인트 ①③ — 고객사 나래비 × 월, 목표 대비 섭외·업로드 + 지연 큐.

- 행 = 고객사, 중앙 = 선택월 (전월/익월은 미니 % 셀). 유형별 진행바 2겹(섭외 옅음 + 업완 짙음) + **월 경과 기준선** — 진도색은 "진도율 vs 월 경과율" 편차 (ok ≥ -5%p / warn / orange / bad < -30%p, admin-targets와 동일 로직)
- 숫자줄 `목·섭·업·취` — **숫자 클릭 = 해당 목록 필터** (섭외 = is-none-of 의미에 맞춰 "취소 뺀 전부")
- 업체명 클릭 → **3개월 요약 블록 먼저**, 블록 요소 클릭 시 세부리스트 (관리화면과 같은 드릴다운 — "전부 한꺼번에 펼쳐짐" 피드백으로 재설계)
- 상태 필터: `섭외지연`(목표 미달 + 경과 대비 -5%p 이상) / `업로드지연`(방문+7일 초과 미제출 보유) / `완료`(섭외≥목표) + 매장 수 뱃지. 업로드지연 필터 중엔 행 안에 지연 건 칩(`인플ID D+n`) 인라인 표시
- 담당자(HH/LH/AN): 헤더 선택 = 지연·대기 숫자와 목록 기본값만 담당자 기준 재계산. **목·섭·업·취는 항상 rollup(정산 기준) 고정** — 목표가 매장 단위라 담당자별 분해는 무의미 + 이중 출처 혼란 방지 (§6-D5). 세부리스트 안에도 HH/LH/AN 전환 버튼(건수 포함)
- 세부리스트 12컬럼: #·담당·유형·상태·방문일시(+변경)·대표인플·인플(클릭=샤오홍슈 홈)·인원건수·제출링크 3종(小红/大众/抖音 칩, hover=URL, 클릭=새탭)·메모(비고 인라인 편집 = 웹의 첫 쓰기)·전달(제출링크·촬영가이드 복사)·기한(D±n)
- ⓘ 업체정보 카드: CS_DB 9개 항목 + 계약비고 + 🔔전달사항 + 전체 복사. `추가OK`(추가체험단)·`🔔`(확인요망) 칩 상시
- 섭외 ≥ 목표면 `✅ 섭외완료 · 다음달 입력` 안내 칩 (Phase 2 가드의 예고편)

### 5.3 `/staff/new` 예약입력 (Phase 2)

**목적**: 페인포인트 ② — 정산월 오입력 원천 차단 + 고객정보 네이티브 표시.

- 좌 폼 / 우 컨텍스트 2단. 우측 상단 = 고객정보(영업·브레이크·휴무·피크·방문가능·제공내역·섭외주의(강조)·촬영대본(복사)) — Softr에서 커스텀 코드로 억지로 박았던 것의 네이티브화
- 우측 하단 = **전월/당월/익월 목표·실적 카드** (목·섭·업·취 + 상태 라벨 + 추가OK + 권장 뱃지). 카드 클릭 = 정산월 선택
- **정산월 3중 가드**:
  1. 기본값 = 계약 있는 달 중 목표 미달인 가장 이른 달 (전월→당월 순서로 탐색)
  2. confirm 2종 — 앞 달 미달인데 뒷 달 선택 시 / 이미 목표 채운 달 선택 시(추가OK 여부 명시)
  3. 서버 = 매장×정산월 Campaign 존재 검증, 없으면 409 ("관리자 화면에서 계약을 먼저") — 없이 만들면 자동화 Find가 빈손 → 귀속 없는 유령 예약이 됨
- 입력 편의: 매장 검색(중문명 포함)·小红건수=총인원 자동 추종(수동 수정 전까지)·대표인플=첫 참여자 자동·참여자 다중 검색 칩
- 신규인플 인라인 모달 (필수 4 + 선택 4). **중복 시 분기**: "입력한 값으로 기존 수정+선택"(updateInfl) / "그대로 선택만"
- 필수(`*` 빨강)/선택 표기 통일, 미충족 시 제출 잠금 + 사유 표시
- 영문이름(과거 잠수함 여권 요건)은 "특수 요청 항목" 접힘으로 보존
- 하향식 입력 계승: 보드 행 ＋버튼 → `?store=<CS recId>` 프리셀렉트

### 5.4 `/staff/queue` 업무·발송 큐 (Phase 3~4)

**목적**: Softr ⑥⑦② 액션 통합. 예약입력_DB 단일 대상.

- 탭: `📤발송대기(예약요청·긴급 & 미체크)` `⏳봇 처리중(체크됨 & 아직 예약요청)` `🔄변경요청` `✅확정·진행(예약확정~송부완료)` `🚫취소·노쇼` `전체` + 담당 필터 + 검색. 창 = 정산월 ±1개월 (실측 273건)
- 카드: 매장·Shoot_ID·상태칩·일시(+변경)·인원·小/大·인플·메모류·**발송문 미리보기(펼치기)** — 변경 계열은 변경메시지 우선(봇 차단 경고문이면 예약메시지로 폴백)
- 액션 (V7 명세 1:1): 전송 / 변경(모달: 변경일시*+변경인원+메모) / 변경확정 / 취소·노쇼(모달: 유형 3종+메모) / 삭제 / **발송취소(unsend)**
- **봇 처리중 = 봇 장애 신호** — 카드 노란 경고 + "오래 머물면 예약봇(PC 앱) 실행 확인" + 발송취소로 대기 복귀 (체크 시각 필드가 없어 경과시간 표시는 불가 — 한계로 기록)
- 삭제 안전장치: 예약요청&미발송만 + 팀명생성기로 자식 조회 → **자식 중 진행 나간 건 있으면 거부** → 자식 10건 청크 삭제 → 부모 삭제

### 5.5 인증 + 파일 구성

- 게이트: `x-staff-key` 헤더 또는 `?k=` URL(Softr iframe 임베드용 — **검증 전 sessionStorage 저장**, StrictMode 이중 실행 버그 수정됨). 서버는 timingSafeEqual + 실패 404 은폐 + 키 미설정 시 503 fail-closed (_admin-auth 패턴)
- 키 위계: `STAFF_KEYS`(개인 `HH:키,…`) > `STAFF_KEY`(공용) > `ADMIN_KEY`/`CLIENTS_ADMIN_KEY`(관리자 상위호환). **현재 Vercel 미설정 → 관리자 키로 입장 가능**
- **키 롤오버 (퇴사·교체) 절차** — 소요 1~2분:
  1. Vercel → 프로젝트 → Settings → Environment Variables → `STAFF_KEYS`에서 해당 `ID:키` 항목 삭제(또는 값 교체) → Save
  2. Deployments → 최신 배포 ⋯ 메뉴 → **Redeploy** (환경변수는 재배포해야 반영된다)
  3. 주의: 이미 열려 있는 탭의 sessionStorage 키는 재배포 순간부터 404로 거부되므로 별도 조치 불요. 다만 재배포 **전**까지는 구 키가 유효하다 — 즉시 차단이 필요하면 재배포를 먼저
- 파일: `api/_staff-auth.js` `staff-check.js` `staff-board.js` `staff-resv.js` `staff-queue.js` / `src/components/StaffGate.jsx` / `src/pages/StaffBoardPage·StaffResvPage·StaffQueuePage.jsx(+css)` / 라우트 `/staff` `/staff/new` `/staff/queue` (App.jsx)

---

## 6. 검토·결정 로그 — 쟁점별 대안·결론·근거

| # | 쟁점 | 검토한 대안 | 결론 | 근거 |
|---|---|---|---|---|
| D1 | 신규예약을 어느 테이블에 쓰나 | (a) 진행_DB_OLD 직접 생성 (b) 예약입력_DB 경유 | **(b)** | (a)는 팀 쪼개기·팀명생성기·봇 캐스케이드를 전부 재구현해야 하고 하나라도 어긋나면 카톡 발송이 깨짐. 자동화를 그대로 태우는 게 유일한 안전 경로 |
| D2 | 지연 판정 기준 | (a) 제출마감일 우선, 없으면 방문+7일 (b) 방문+7일 단일 | **(b)** (Owner 확정 8/4) | 기준이 둘이면 담당자마다 다르게 읽음. `UPLOAD_GRACE_DAYS=7` 상수 하나로 조정 가능하게 |
| D3 | 보드 숫자의 출처 | (a) 항상 rollup (b) 항상 건 목록 클라 계산 (c) 혼합 | **(c)→(a) 수렴** | 초기 혼합(담당자 필터 시 재계산)이 혼란 유발. 최종: 목·섭·업·취=rollup 고정(관리화면·정산과 일치), 지연·대기만 담당자 기준 재계산 (rollup은 담당자 분해 불가) |
| D4 | 보드 범위 | (a) 전 유형 (b) 체험+기자 (c) 체험만 | **(c)** (Owner 8/4) | 체험단 섭외담당 화면. FB는 인플 입력용 계정이라 담당자 버튼에서도 제외 (기록의 담당 열엔 표시) |
| D5 | 담당자 필터 위치 | (a) 헤더 전역 (b) 세부리스트 안 (c) 둘 다 | **(c)** (Owner 8/5) | (a)만 뒀을 때 "필터 바꾸면 보던 리스트 닫힘" 문제 → 리스트 유지 로직 + 헤더 선택이 리스트 기본값으로 이어지는 구조. 중간에 (a)를 제거했다가 복원한 이력 있음 — 최종형이 (c) |
| D6 | 정산월 오입력 방지 강도 | (a) 안내만 (b) UI 경고 (c) 서버 거부 | **전부 (3중)** | 오입력 비용(고객 보고서 오류)이 입력 편의보다 훨씬 큼. 단 서버 거부는 "계약 없음"만 — 미달/초과는 confirm으로 사람이 넘을 수 있게 (운영 유연성) |
| D7 | 유령계약(계약유형 공란) 처리 | (a) admin-targets처럼 전부 제외 (b) 숫자 있으면 포함 (c) 계약유형 완전 무시 | **(c)** (Owner 8/4 "계약유형은 관리자 관심사") | (a)를 그대로 복사했다가 실측에서 8월 95건 중 75건 공란·그중 5건 실적 보유 발견 — 살아있는 매장 5곳이 화면에서 사라져 있었음. 목표·실적 전부 0인 껍데기만 제외 |
| D8 | 취소·노쇼 경로 이원화 (F1) | (a) 현행 유지 (진행_DB_OLD 직접 + 봇 경로 공존) (b) 봇 경로 단일화 | **(b)** (Owner 8/5) | "발송되는 취소"와 "기록만 취소"가 공존하면 고객 안내 누락 발생. 봇이 취소 안내를 일정하게 보내고 있고, DB 미기록 문제는 달력 복원(_resv-message.js)으로 이미 해소 |
| D9 | 매장×월 계약 조회 방법 | (a) 고객사명+지점명 문자열 매칭 (b) CS_DB 링크 필드 | **(b)** | 이름 매칭은 공백·표기 차이로 틀린 매장을 묾 (admin-targets ensureCampaign 주석의 실증). CS_DB의 `Campaign_DB`+`Campain_DB`(오타 쌍둥이) 링크 둘 다 조회 |
| D10 | 정산월 이동(월 바꾸기) 방식 (F2) | (a) 정산월만 변경 (b) 정산월 변경+귀속 링크 비움→자동화 재실행 대기 (c) 정산월+귀속 동시 쓰기 | **(c)** (Owner 승인 8/5) | (a)는 유령 제조 (도시어 Q2). (b)는 Owner 제안이었으나 자동화 재실행이 보장 안 됨(트리거=수식 완성 시점) → 검증된 admin-targets `moveSettlement` 로직 재사용. 큐 버튼은 추가 예정 |
| D11 | 발송 전 삭제 (F3) | (a) 삭제 금지, 취소로만 (b) 조건부 삭제 | **(b)** (Owner 8/5) | 발송 화면이 "예약요청&미발송"만 보여주므로 고객에게 나간 적 없음 → 삭제 무방. 단 분할된 자식 고아 방지(동반 삭제) + 자식 중 진행 나간 건 있으면 거부 |
| D12 | 로그인 방식 | (a) 이메일+비번 자체 구현 (b) Clerk 등 인증 서비스 (c) 개인별 키 | **(c) 지금, (b) 나중** (Owner 협의 8/5) | 5~8명 규모에서 (a)는 해싱·재설정·방어를 자체 부담 — 유지보수·보안 비용 과다. (c)는 키=신원이라 감사 가능, 인원 10+·쓰기 확대 시 (b) 전환. admin 키 재사용은 권한 분리 붕괴라 기각, STAFF_KEY 분리 |
| D13 | 봇 처리중 정체 감지 | (a) 경과시간 표시 (b) 경고+발송취소 | **(b)** | 체크 시각 필드가 DB에 없어 (a) 불가 (Last modified 필드 신설은 Airtable 쪽 작업이라 보류). "처리중에 머묾=봇 문제" 신호 가시화 + 대기 복귀 수단 제공 |
| D14 | 영문이름 필드 | (a) 삭제 (b) 일반 필드 유지 (c) 접힘 보존 | **(c)** (Owner 8/5) | 잠수함 여권 요건은 종료됐지만 유사 비정형 요청 대비 |
| D15 | Softr와의 관계 | (a) 즉시 대체 (b) 병행 후 전환 | **(b)** | 각 Phase마다 Softr에 iframe 임베드 가능(`?k=`), 안정화 후 직접 URL 전환, 최종 Softr 해지 검토 |

---

## 7. API 레퍼런스 (게이트: 전부 staffIdentity 통과 필요)

### GET `/api/staff-board?month=2026. 8월`
Campaign_DB ±1개월 + 계약이 문 진행 건 + CS_DB 정보 조인.
응답: `{months[3], el{월:경과율}, rows[{n, p(협력사), info{sid,cn,open,brk,peak,rest,visitOk,give,warn,note}, m{월:{ct,memo,add,chk,notice,t{체험:[목,방,업,취],…}, d[건 객체], pend, late}}}], who}`
건 객체: `{id,sid,mgr,sub,st,ty,mon,visit,chg,lead,infl,ilink,rx,rd,ry,pax,paxChg,nx,nd,memo,give,guide,dl}` (dl = 기한까지 남은 날, 음수=지연)

### POST `/api/staff-board` `{action:'memo', id, memo}` — 진행_DB_OLD.비고만 (1,000자 제한)

### GET `/api/staff-resv?mode=meta` — 매장 129(사용 우선 정렬)+인플 966+선택지
### GET `/api/staff-resv?store=rec…&month=` — CS 정보 + 3개월 가드 `{info, months, byMonth{월:{exists,tg,vis,up,cx,add}}, suggest}`
### POST `/api/staff-resv`
- `{action:'create', store,mgr,type,status,month,when(KST "YYYY-MM-DDTHH:mm"),pax,nx,nd,lead,infls[],paxMemo,clientMemo,engNames,note}` — 가드 통과 시 예약입력_DB 생성. 409: 계약 없음
- `{action:'createInfl', xid,link,pal,mgr,type,wc,phone,nick}` — 중복 시 409+`dupId`
- `{action:'updateInfl', id, …}` — 채워진 필드만 PATCH

### GET `/api/staff-queue` — 정산월 ±1개월 전 건 `{months, items[{id,sid,mgr,ty,st,mon,store,when,chgWhen,pax,chgPax,nx,nd,infls,leadWc,paxMemo,clientMemo,note,msg,chgMsg,sent,created}], who}`
### POST `/api/staff-queue`
- `send` (예약요청·긴급만) / `unsend` (예약요청·긴급·변경요청 & 미처리만)
- `modify {when*, pax?, memo?}` → 변경일시+변경인원+변경요청+체크
- `confirmChange` (변경요청에서만) / `cancel {kind: 취소_방문자|취소_고객사|노쇼, memo?}`
- `remove` — 예약요청&미발송 + 자식 전원 예약요청일 때만, 자식 동반 삭제

공통: 월 표기 `"2026. 8월"` · KST↔UTC 변환 서버 처리 · rate limit 대응 = 80건 청크 × 동시 4
· CORS 헤더 없음(같은 오리진 전용) · choice/option 오류는 "옵션을 먼저 추가하라"로 번역

---

## 8. TRAPS — 사고 이력 포함

| 함정 | 실증 |
|---|---|
| 필드명 공백 (`입력 정산월` 등) | 941건이 "비어있음"으로 오집계된 사고 (도시어) |
| `정산월`·`계약월` singleSelect 옵션 부재 | 2026. 8월까지만 존재. 9월 되면 Airtable에서 수동 추가 (API 불가). INVALID_MULTIPLE_CHOICE_OPTIONS를 사람 말로 번역해 둠 |
| 계약유형으로 거르면 안 됨 | 8월 75/95 공란, 실적 보유 5건 소실됐었음 (D7) |
| rollup 조건은 is-none-of | 진행상태 빈값 173건 때문. 포함 방식으로 세면 다 빠짐 |
| `비고`는 richText | 마크다운 이스케이프(`2026\.`) — 표시 시 unescape, 쓰기는 plain 문자열 OK |
| CS_DB 필드명 접미 | `고객사명(필수)` `영업시간(필수)` — 접미까지가 필드명 |
| CS_DB 캠페인 링크 오타 쌍둥이 | `Campaign_DB` + `Campain_DB` 둘 다 봐야 전 건 조회됨 |
| StaffGate `?k=` StrictMode | 검증 후 저장 방식은 dev 이중 실행에서 잠김 → 검증 전 저장으로 수정 (커밋 `6f8c1f8`) |
| 예약입력_DB 쓰기 = 실탄 | 자동화 분할 + 봇 카톡 실발송. 스모크는 검증 경로만 테스트했고 실제 생성 테스트는 통제 1건으로 해야 함 |
| 대표인플 채널링크 없음 | 진행_DB_OLD에 대표인플의 XHS 홈 룩업 부재 → 텍스트 표시만. 원하면 Airtable에 룩업 1개 추가 후 연결 |
| eslint flat config | `/* eslint-env node */` 주석 무효 — eslint.config.js files 목록에 넣어야 함 (dev-api.js 누락 사고) |
| **CSS 클래스에 'ad/ads' 금지** | 고객사 등록 화면의 `ads-` 접두사가 애드블록 확장의 일반 필터에 걸려 **목록·에러 배너가 통째로 숨겨짐** (2026-08-06 실사고 — 확장 없는 브라우저에선 정상이라 원인 규명에 오래 걸림). 에러 표시까지 숨겨져 "조용한 빈 화면"이 됨. `cst-`로 전면 교체. 클래스·id에 ad, ads, banner, sponsor 류 단어 금지 |
| 스키마 덤프는 낡는다 | `_raw_schema.json`(8/3)에 8/4 신설 필드(`대표인플_ID`) 없음 — 실측 우선 |
| 발송취소(unsend)의 잔여 레이스 창 | 서버는 상태 기반으로 방어(봇 처리 완료 후엔 409)하지만, **봇이 집어간 순간~상태 PATCH 사이 수 초**는 취소가 무시되고 발송될 수 있다. 봇 측 "처리중 claim" 필드 없이는 원천 차단 불가 — UI가 경고 문구 + 취소 후 확정·진행 탭 이동 여부 확인을 안내 (AG 리뷰 지적) |
| 봇 처리 중 발송대기 재등장 | 봇은 수집 즉시 체크를 해제하므로, **처리 중(해제~상태 PATCH 사이)에는 웹에서 다시 발송대기로 보인다** — 배치가 길면 수 분. 이때 전송을 다시 누르면 이중 발송이 된다. 방금 전송한 건이 대기로 돌아와 보여도 재클릭 금지, 1~2분 뒤 새로고침으로 확인 |
| V7 README는 일부 낡음 | 변경확정 정책이 README(V6 초기: 예약일시 이관)와 코드(V6.3: 원본 유지·자동확정)가 다르다 — **코드가 진실.** 봇 동작은 README 아닌 TK_Resv_V7.py 실측 기준으로 |
| Airtable 429 | staff API 3종의 `at()`에 지수 백오프 재시도 내장 (0.4→0.8→1.6s+지터, 최대 3회). 429는 미실행 응답이라 쓰기도 재시도 안전, 5xx는 GET만 재시도(중복 생성 방지) (AG 리뷰 반영) |

---

## 9. 현재 상태·검증 체크리스트·리스크

**배포 상태** (2026-08-05, 커밋 `6f8c1f8`): 3화면 전부 라이브. lint 0건·빌드 통과·실데이터 렌더 검증(캡처 5장). 실제 쓰기 중 검증 완료된 것 = 메모 저장뿐.

**체크리스트 (순서대로)**
- [ ] **통제 실전 테스트 1건**: /staff/new 생성 → 예약입력_DB·자동 분할·귀속 정산월 링크 확인 → /staff/queue 전송 → 봇 발송·캐스케이드 확인 → (별건으로) 삭제 흐름 검증
- [ ] Vercel `STAFF_KEY` 설정 → 담당자 배포 (개인키 `STAFF_KEYS`는 인원 확정 후)
- [ ] 큐에 정산월 이동 버튼 (D10 로직)
- [ ] 인플 조회 전용 페이지 필요 여부 (현재 폼 검색 966명으로 대체 중)
- [ ] **모바일 실기기 테스트** (AG 권고): 폰에서 큐 변경 모달의 datetime 입력·취소 모달 키보드 팝업 시 UI 가림, 보드 세부리스트 메모 편집, 예약폼 인플 검색 드롭다운. CSS는 모바일에서 모달 상단 정렬로 보강했으나 실기기 확인 필요
- [x] 고객등록 화면 — `/admin/stores` (2026-08-06, 커밋 `eadc9b7`): CS_DB 등록·수정 + 계약·목표 upsert. 업체명 링크 직결로 신규 고객사 첫 계약 생성 가능 (ensureCampaign 한계 해소)
- [ ] Softr 병행 1~2주 → 전환 → Softr·레거시 /manager 정리 (무인증+CORS `*` 청산 겸)

**리스크**
- 큐 액션과 Softr 버튼이 병행 기간 동안 공존 — 같은 필드를 켜는 것이라 충돌은 없지만, **봇 이중 실행(V5/V6/V7 동시)** 은 중복 발송이므로 금지 (V7 README 경고)
- 예약입력_DB 삭제(Softr ⑥의 Delete 버튼)는 Softr에 여전히 살아 있음 — 웹 삭제와 달리 자식 정리를 안 하므로 병행 기간엔 Softr 삭제 자제 권장
- 9월 초 singleSelect 옵션 추가를 잊으면 폼 생성이 멈춤 (에러 문구는 안내형으로 준비됨)

---

## 10. 스태프 아케이드 「인플 캐치!」 (2026-08-06, Owner 발주)

/staff 진도보드 맨 아래. 섭외 실무의 고통(먹튀·노쇼·지각·업로드 잠수)을 유쾌하게
형상화한 캐치 게임 — 직원 사기 진작용. **접힌 배너가 기본**이라 업무 화면을 침범하지 않는다.

- **파일**: `src/lib/catchGame.js`(캔버스 엔진) + `src/components/StaffGame.jsx/.css`(UI·리더보드) + `api/staff-game.js`
- **저장소**: Airtable `Game_DB` (tblAso7MZHFW3cNNn, 2026-08-06 메타 API로 신설).
  필드 Key·ID·Score·Game·PlayedAt. **개인당 상위 5건만 유지**(초과분 서버가 삭제), 점수 상한 20만.
- **신원 = 키**: staffIdentity 가 주는 who(HH/LH/AN/STAFF/ADMIN)가 그대로 리더보드 ID.
  클라이언트가 ID 를 조작할 수 없다. 랭킹 서버가 죽어도 게임은 되고 최고점만 localStorage 폴백.
- **조작**: ←→·마우스·터치. Space 시작/재시작. **Esc = 보스키(즉시 접힘)**. 효과음 기본 꺼짐(WebAudio 합성).
- **메커닉**(인기 게임 조사 반영): 원축 조작+짧은 라운드+즉시 재시작(Dino/Flappy 공식),
  5연속 콤보 배수(최대 3배), 희귀 보상(👑+500·🧧+88·☕하트+1 = 가변 보상), 20초당 난이도 상승.
  **먹튀(😈)는 착한 인플(🤳)로 위장했다가 캐치 직전(놀람 연출)에 본색** — 실무 그대로.
  노쇼(👻)는 흔들리며 낙하, 길 잃은 인플(😵)은 갈지자로 헤매고, 딴 지점 인플(🏃)은 낙하 중 순간이동.
- **잭팟·시나리오 (2026-08-06 2차 확장, Owner 요청)**: 👑 대형 인플 체험 섭외 = 잭팟 배너+컨페티+효과음.
  🧧 홍바오 = 红包雨 코스메틱 낙하. 배경에 "사장님이 통역까지 불러놓고 대기 중… 위챗은 진동벨 수준"
  류 시나리오 멘트가 9~16초마다 순환(캐치와 무관, 트렌디 중국어 已读不回·绝绝子·yyds·666 섞음).
  15~25초마다 **동료 위챗 알림 카드**(우상단 슬라이드인, 3.4초)가 랜덤 등장해 버프: HH 엘레인(콜찬스·
  캐치범위 UP) · LH 해피맘(부케 부적·하트/보너스) · AN 프리지아(好运 타임·희귀템 UP) · QN 金姐 사장님
  (점수 배수 UP) · GG Andy 이사(캐치 보너스 +30/회). **5명 모두 실제 위챗 프로필의 페르소나를 재미있게
  응용한 것**(사진·실명 그대로 쓰지 않음) — `BUDDIES` 상수(catchGame.js)에서 이름·멘트 관리.
- **함정**: 게임 점수 API 도 `_staff-auth.js` 게이트 뒤라 STAFF_KEY/ADMIN 키 없으면 404 —
  리더보드만 실패하고 게임은 정상(의도된 동작). 로컬 dev-api 는 프로덕션과 같은 Airtable 베이스를
  보므로, dev 서버로 플레이해도 실제 리더보드에 기록이 쌓인다(2026-08-06 GG 실플레이로 확인) — 테스트
  플레이는 되도록 프로덕션에서, dev 서버 테스트 점수는 필요 시 Airtable에서 직접 삭제.

---

## 11. QR 입장 체크인 (2026-08-06, v1.2 배포됨)

상세 스펙·경위는 `QR_CHECKIN_SPEC.md` (v1.2). 요점만:

- **경로**: 매장 QR(URL) → 인플이 **위챗 扫一扫**로 스캔 → `/checkin?s=&t=` 자동 체크인
  (신원 = /submit 방문 시 localStorage에 심는 `tk_submit_token`). 백업 = 매장별 **6자리
  숫자 코드**(/submit 체크인 모달에서 입력, /schedule QR 모달·admin QR 다운로드 시 표기).
- **사진 촬영→웹 디코드는 폐기** — 모니터 모아레를 엔진 교체(jsQR·ZXing·BarcodeDetector)로
  못 이긴다는 AG 실기기 검증 결과. `html5-qrcode`·`jsqr` 의존성도 제거됨.
- **서명 일원화**: `api/_qr-sign.js` (`storeSig`·`storeCode6`). 시크릿 없으면 빈 값
  (fail-closed, QR UI 자동 숨김). `QR_CHECKIN_SECRET`은 Vercel에 **등록 확인됨**
  (2026-08-06 프로덕션 프로브: 코드 오입력 404 응답 = 코드 경로 실행됨).
- **효과**: 진행_DB_OLD `체크인일시` + 상태(예약요청·예약확정·긴급예약·변경확정→촬영완료),
  예약입력_DB `체크인내용` append + `체크인알림대기` 체크. 봇 V8(카톡 알림)은 2단계 —
  스펙 §8, 아직 미착수.
- **남은 검증**: 위챗 실기기 통제 테스트 1건 (스펙 §10 시나리오).
- **함정**: `tamkorea.com` → `www.tamkorea.com` 307 리다이렉트 존재. GET(QR 스캔)은
  쿼리 보존되어 무해하나, 외부에서 API를 직접 POST로 칠 때는 www 호스트를 써야 한다.
